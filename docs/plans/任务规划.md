# AI Agent 智能体开发 — 任务规划

**版本：** 1.3.1  
**日期：** 2026-05-24  
**状态：** 开发中 (Phase 0-3 ✅ · Phase 5 🔧 2/6 · Phase 4 📋)  
**项目：** AI校园墙智能平台

---

## 一、项目背景

本项目为 **AI校园墙智能平台**，依托人工智能技术打造校园一体化交流服务社区。支持用户发布表白、寻物、求助、校园资讯、生活吐槽等动态内容；集成 AI 智能内容审核、AI 违规识别、智能推荐、智能分类功能。

---

## 二、模块总览

```
AI治理 (父菜单)
├── LLM大模型配置    [✅ Phase 0 已完成]
├── 提示词管理        [✅ Phase 1 已完成]
├── 知识库管理        [✅ Phase 2 已完成]
├── AI Agent 控制台   [✅ Phase 3 已完成]
└── 工具与插件管理    [📋 Phase 4 待开发]
```

---

## 三、Phase 0: LLM大模型接入 ✅ 已完成

```
已完成内容:
├── 8种主流服务商 (通义千问/Deepseek/OpenAI/Anthropic/Google/豆包/智谱AI/OpenRouter)
├── BaseURL 编辑与自动适配
├── API Key 安全管理
├── 模型测试接入与选择
├── 前端卡片式管理界面
└── 完整 RESTful API
```

---

## 四、Phase 1: 提示词管理 ✅ 已完成

> **完成时间：** 2026-05-23  
> **编译状态：** BUILD SUCCESS  
> **需求对齐度：** 核心功能 100%，详见下方偏差说明

### 4.1 功能目标 ✅

- ✅ 支持编写系统提示词和用户提示词
- ✅ 支持提示词占位符变量 `{{variable}}`
- ✅ 支持一键 AI 生成/润色提示词
- ✅ 内置校园墙场景专属模板
- ✅ 支持提示词模板的 CRUD 和启用/禁用

### 4.2 数据库设计

```sql
-- 提示词模板表
CREATE TABLE ai_prompt_template (
  template_id     BIGINT(20)   NOT NULL AUTO_INCREMENT  COMMENT '模板ID',
  template_name   VARCHAR(100) NOT NULL                 COMMENT '模板名称',
  template_type   CHAR(1)      NOT NULL DEFAULT '0'     COMMENT '类型(0系统提示词 1用户提示词)',
  scene_code      VARCHAR(50)  DEFAULT NULL             COMMENT '场景代码(content_audit/recommend/classify/chat)',
  scene_name      VARCHAR(100) DEFAULT NULL             COMMENT '场景名称',
  prompt_content  TEXT         NOT NULL                 COMMENT '提示词内容',
  variables       VARCHAR(500) DEFAULT NULL             COMMENT '变量列表(JSON数组)',
  system_prompt_id BIGINT(20)  DEFAULT NULL             COMMENT '关联系统提示词ID(用户提示词时可关联)',
  status          CHAR(1)      DEFAULT '0'              COMMENT '状态(0启用 1停用)',
  is_builtin      CHAR(1)      DEFAULT '0'              COMMENT '是否内置(0否 1是)',
  sort            INT(4)       DEFAULT 0                COMMENT '显示顺序',
  remark          VARCHAR(500) DEFAULT NULL             COMMENT '备注',
  create_by       VARCHAR(64)  DEFAULT ''               COMMENT '创建者',
  create_time     DATETIME                              COMMENT '创建时间',
  update_by       VARCHAR(64)  DEFAULT ''               COMMENT '更新者',
  update_time     DATETIME                              COMMENT '更新时间',
  PRIMARY KEY (template_id)
) ENGINE=InnoDB COMMENT='AI提示词模板表';

-- 提示词生成历史表
CREATE TABLE ai_prompt_generation (
  gen_id          BIGINT(20)    NOT NULL AUTO_INCREMENT  COMMENT '生成ID',
  user_input      TEXT          NOT NULL                 COMMENT '用户输入的需求描述',
  generated_prompt TEXT         NOT NULL                 COMMENT 'AI生成的提示词',
  template_type   CHAR(1)       DEFAULT NULL             COMMENT '生成类型(0系统提示词 1用户提示词)',
  llm_config_id   BIGINT(20)    DEFAULT NULL             COMMENT '使用的LLM配置ID',
  model_name      VARCHAR(100)  DEFAULT NULL             COMMENT '使用的模型名称',
  create_by       VARCHAR(64)   DEFAULT ''               COMMENT '创建者',
  create_time     DATETIME                               COMMENT '创建时间',
  PRIMARY KEY (gen_id)
) ENGINE=InnoDB COMMENT='提示词生成历史表';
```

### 4.3 内置模板（校园墙场景）

| 场景代码 | 场景名称 | 模板类型 | 状态 |
|---------|---------|---------|:--:|
| content_audit | AI内容审核 | 系统 | ✅ 已内置 |
| content_classify | AI智能分类 | 系统 | ✅ 已内置 |
| content_recommend | AI智能推荐 | 系统 | ✅ 已内置 |
| campus_chat | 校园智能助手 | 系统 | ✅ 已内置 |
| comment_moderate | AI评论管理 | 系统 | ⏸️ 待补全 |
| user_report | 举报分析 | 系统 | ⏸️ 待补全 |
| *(额外)* | 内容审核(用户提示词) | 用户 | ✅ 额外补充 |
| *(额外)* | 智能分类(用户提示词) | 用户 | ✅ 额外补充 |

> **偏差说明：** 规划6个内置系统提示词模板，实际实现4个 + 2个额外用户提示词模板。comment_moderate 和 user_report 模板的 prompt_content 待补充。

### 4.4 前端页面结构 (实际)

```
views/ai/prompt/
└── index.vue              # 提示词管理主页面（卡片式，组件内联合并）
                            #   实际将 PromptCard + PromptEditor + PromptGenerator 合并为单文件
                            #   包含: 场景选择卡片、CRUD表单对话框、AI生成/润色对话框
```

> **偏差说明：** 规划拆分为 PromptCard / PromptEditor / PromptGenerator 三个独立组件，实际合并为单文件开发。功能完整，后续可按需拆分。

### 4.5 API设计

| 接口 | 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|:--:|
| 列表 | GET | /ai/prompt/list | 提示词模板列表 | ✅ |
| 系统提示词 | GET | /ai/prompt/systemPrompts | 启用的系统提示词(下拉用) | ✅ |
| 详情 | GET | /ai/prompt/{id} | 获取模板详情 | ✅ |
| 新增 | POST | /ai/prompt | 新增模板 | ✅ |
| 修改 | PUT | /ai/prompt | 修改模板 | ✅ |
| 删除 | DELETE | /ai/prompt/{ids} | 删除模板 | ✅ |
| AI生成 | POST | /ai/prompt/generate | 调用LLM生成/润色提示词 | ✅ |
| 预览 | POST | /ai/prompt/preview | 预览替换变量后的提示词 | ⏸️ 待补 |
| 变更状态 | PUT | /ai/prompt/changeStatus | 启用/停用 | ⏸️ 待补 |

> **偏差说明：** `/preview` 和 `/changeStatus` 两个端点未实现。当前通过完整 CRUD 中的 `status` 字段间接支持状态变更，预览功能暂未开发。

### 4.6 后端文件清单 (实际)

```
ruoyi-system/src/main/java/com/ruoyi/system/
├── domain/
│   ├── AiPromptTemplate.java          ✅
│   ├── AiPromptGeneration.java        ✅
│   └── vo/
│       └── AiPromptTemplateQuery.java ✅
│       ✗ PromptGenerateVO.java        (合并到AiPromptTemplate.remark传mode)
├── mapper/
│   ├── AiPromptTemplateMapper.java    ✅
│   └── AiPromptGenerationMapper.java  ✅
├── service/
│   ├── IAiPromptTemplateService.java  ✅ (含generatePrompt方法)
│   └── impl/
│       └── AiPromptTemplateServiceImpl.java ✅
│   ✗ IAiPromptGenerateService.java   (合并到IAiPromptTemplateService)
│   ✗ AiPromptGenerateServiceImpl.java (合并到AiPromptTemplateServiceImpl)
└── controller/system/
    └── AiPromptController.java        ✅

resources/mapper/system/
├── AiPromptTemplateMapper.xml         ✅
└── AiPromptGenerationMapper.xml       ✅
```

> **偏差说明：** IAiPromptGenerateService 和 PromptGenerateVO 未独立创建，生成/润色功能合并到 IAiPromptTemplateService 中，通过 remark 字段传递模式。

### 4.7 权限标识

| 权限标识 | 说明 |
|---------|------|
| ai:prompt:list | 查看提示词列表 |
| ai:prompt:add | 新增提示词 |
| ai:prompt:edit | 编辑提示词 |
| ai:prompt:remove | 删除提示词 |
| ai:prompt:generate | AI生成/润色 |

---

## 五、Phase 2: 知识库管理 ✅ 已完成

> **完成时间：** 2026-05-23  
> **编译状态：** BUILD SUCCESS  
> **需求对齐度：** 核心功能 100%，向量化+语义搜索已由 Phase 3 补全

### 5.1 功能目标 ✅

- ✅ 支持多知识库管理
- ✅ 支持文件上传解析（txt/pdf/docx/xlsx/md）
- ⚠️ 图片上传暂不支持（OCR，Phase 5 实现）
- ✅ 文件内容自动解析和文本提取 (PDFBox + POI)
- ✅ 向量化存储 — Phase 3 通过 EmbeddingServiceImpl 完成
- ✅ 语义搜索 — Phase 3 通过 EmbeddingServiceImpl (余弦相似度) 完成
- ✅ Agent RAG 集成 — Phase 3 通过 AgentEngine + EmbeddingService 完成

### 5.2 数据库设计

```sql
-- 知识库表
CREATE TABLE ai_knowledge_base (
  kb_id           BIGINT(20)   NOT NULL AUTO_INCREMENT  COMMENT '知识库ID',
  kb_name         VARCHAR(100) NOT NULL                 COMMENT '知识库名称',
  kb_desc         VARCHAR(500) DEFAULT NULL             COMMENT '知识库描述',
  embedding_model VARCHAR(100) DEFAULT NULL             COMMENT '向量化模型',
  doc_count       INT(11)      DEFAULT 0                COMMENT '文档数量',
  chunk_count     INT(11)      DEFAULT 0                COMMENT '分块总数',
  status          CHAR(1)      DEFAULT '0'              COMMENT '状态(0正常 1停用)',
  create_by       VARCHAR(64)  DEFAULT ''               COMMENT '创建者',
  create_time     DATETIME                              COMMENT '创建时间',
  update_by       VARCHAR(64)  DEFAULT ''               COMMENT '更新者',
  update_time     DATETIME                              COMMENT '更新时间',
  PRIMARY KEY (kb_id)
) ENGINE=InnoDB COMMENT='AI知识库表';

-- 知识库文档表
CREATE TABLE ai_knowledge_document (
  doc_id          BIGINT(20)   NOT NULL AUTO_INCREMENT  COMMENT '文档ID',
  kb_id           BIGINT(20)   NOT NULL                 COMMENT '知识库ID',
  doc_name        VARCHAR(200) NOT NULL                 COMMENT '文档名称',
  doc_type        VARCHAR(20)  DEFAULT NULL             COMMENT '文档类型(txt/pdf/docx/xlsx/md/jpg/png)',
  file_path       VARCHAR(500) DEFAULT NULL             COMMENT '文件存储路径',
  file_size       BIGINT(20)   DEFAULT 0                COMMENT '文件大小(字节)',
  content_text    LONGTEXT     DEFAULT NULL             COMMENT '提取的文本内容',
  chunk_count     INT(11)      DEFAULT 0                COMMENT '分块数量',
  status          CHAR(1)      DEFAULT '0'              COMMENT '状态(0处理中 1完成 2失败)',
  error_msg       VARCHAR(500) DEFAULT NULL             COMMENT '错误信息',
  create_by       VARCHAR(64)  DEFAULT ''               COMMENT '创建者',
  create_time     DATETIME                              COMMENT '创建时间',
  PRIMARY KEY (doc_id),
  KEY idx_kb_id (kb_id)
) ENGINE=InnoDB COMMENT='AI知识库文档表';

-- 文档分块表
CREATE TABLE ai_document_chunk (
  chunk_id        BIGINT(20)   NOT NULL AUTO_INCREMENT  COMMENT '分块ID',
  doc_id          BIGINT(20)   NOT NULL                 COMMENT '文档ID',
  kb_id           BIGINT(20)   NOT NULL                 COMMENT '知识库ID',
  chunk_index     INT(11)      NOT NULL                 COMMENT '分块序号',
  chunk_content   TEXT         NOT NULL                 COMMENT '分块文本',
  chunk_vector    JSON         DEFAULT NULL             COMMENT '向量数据(JSON格式)',
  token_count     INT(11)      DEFAULT 0                COMMENT 'Token数量',
  PRIMARY KEY (chunk_id),
  KEY idx_doc_id (doc_id),
  KEY idx_kb_id (kb_id)
) ENGINE=InnoDB COMMENT='文档分块表';
```

### 5.3 技术方案 (实际)

```
文件上传 → 文本提取 → 文本分块(500字/块 + 50字重叠) → 存入数据库
         → [Phase 3 ✅] 向量化 → [Phase 3 ✅] 语义搜索 → [Phase 3 ✅] LLM汇总
```

**文件解析策略 (已实现)：**
| 文件类型 | 解析方式 | 状态 |
|---------|---------|:--:|
| .txt | 直接读取 | ✅ |
| .md | 直接读取 | ✅ |
| .pdf | Apache PDFBox 3.0.5 | ✅ |
| .docx | Apache POI (XWPFWordExtractor) | ✅ |
| .xlsx | Apache POI (XSSFWorkbook) | ✅ |
| .jpg/.png | Tesseract CLI OCR | ⏸️ Phase 5 |

> **技术选型记录 (2026-05-23)：**
> - PDF 解析：Apache PDFBox (推荐A方案) ✅
> - OCR 支持：Tesseract CLI (Phase 5) ✅
> - 向量化存储：Phase 3 与 Agent 同步实现 (推荐B方案) ✅

### 5.4 前端页面结构 (实际)

```
views/ai/knowledge/
└── index.vue                # 单文件页面（卡片式知识库 + 抽屉文档管理 + 上传 + 检索 + 分块查看）
                              #   实际将 KnowledgeCard + DocumentList + UploadDialog 合并
                              #   功能: 知识库卡片列表、文档抽屉列表、文件上传、关键词检索、分块查看
```

> **偏差说明：** 规划拆分为3个组件，实际合并为单文件。功能完整：含卡片式知识库切换、el-drawer 文档面板、el-upload 文件上传、分块查看对话框。

### 5.5 API设计

| 接口 | 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|:--:|
| 列表 | GET | /ai/knowledge/base/list | 知识库列表 | ✅ |
| 详情 | GET | /ai/knowledge/base/{id} | 知识库详情 | ✅ |
| 新增 | POST | /ai/knowledge/base | 新增知识库 | ✅ |
| 修改 | PUT | /ai/knowledge/base | 修改知识库 | ✅ |
| 删除 | DELETE | /ai/knowledge/base/{ids} | 删除知识库 | ✅ |
| 文档列表 | GET | /ai/knowledge/doc/list | 文档列表 | ✅ |
| 上传文档 | POST | /ai/knowledge/doc/upload | 上传文档(multipart) | ✅ |
| 文档详情 | GET | /ai/knowledge/doc/{id} | 文档+分块详情 | ✅ |
| 删除文档 | DELETE | /ai/knowledge/doc/{ids} | 删除文档 | ✅ |
| 重新解析 | POST | /ai/knowledge/doc/reparse/{id} | 重新解析文档 | ✅ |
| 检索 | POST | /ai/knowledge/search | 关键词检索 | ✅ |

### 5.6 后端文件清单 (实际)

```
ruoyi-system/src/main/java/com/ruoyi/system/
├── domain/
│   ├── AiKnowledgeBase.java           ✅
│   ├── AiKnowledgeDocument.java       ✅
│   └── AiDocumentChunk.java           ✅
├── mapper/
│   ├── AiKnowledgeBaseMapper.java     ✅
│   ├── AiKnowledgeDocumentMapper.java ✅
│   └── AiDocumentChunkMapper.java     ✅
├── service/
│   ├── IAiKnowledgeBaseService.java   ✅
│   ├── IAiKnowledgeDocumentService.java ✅
│   ├── IAiDocumentParserService.java  ✅ (核心解析引擎)
│   └── impl/
│       ├── AiKnowledgeBaseServiceImpl.java   ✅
│       ├── AiKnowledgeDocumentServiceImpl.java ✅
│       └── AiDocumentParserServiceImpl.java  ✅ (PDFBox+POI解析)
└── controller/system/
    └── AiKnowledgeController.java     ✅ (12个API端点)

resources/mapper/system/
├── AiKnowledgeBaseMapper.xml          ✅
├── AiKnowledgeDocumentMapper.xml      ✅
└── AiDocumentChunkMapper.xml          ✅

sql/ (建表脚本)
├── ai_knowledge.sql        ✅ (3张表：base/document/chunk)
└── ai_knowledge_menu.sql   ✅ (菜单+权限)
```

> **完全对齐规划，无偏差。**

---

## 六、Phase 3: AI Agent 控制台 ✅ 已完成

> **完成时间：** 2026-05-23  
> **编译状态：** BUILD SUCCESS  
> **需求对齐度：** 核心功能 100%，tool/ 子模块延后至 Phase 4，详见偏差说明

### 6.1 功能目标 ✅

- ✅ 对话式前端界面（三栏布局），与 Agent 实时交互
- ✅ 多轮对话记忆能力 — MemoryManager 短期窗口管理 + 摘要压缩
- ✅ 支持选择 LLM 配置和提示词模板
- ✅ 知识库挂载（RAG 增强） — EmbeddingServiceImpl 语义搜索 + 余弦相似度
- ✅ 对话历史管理 — 新建/切换/删除会话，历史持久化
- ⚠️ 工具调用结果展示 — 前端 ToolResultCard 已就位，后端 tool/ 子模块未实现
- ⚠️ 长期记忆自动功能 — 仅建表+基础CRUD，自动提取/注入标注 Phase 5

### 6.2 实际调用链路

```
前端 index.vue → fetch API (SSE流式)
  → AiAgentController.chat()
    → AiAgentServiceImpl.chat() (189行核心编排)
      → 加载会话/LLM配置/系统提示词/历史消息
      → EmbeddingServiceImpl.searchInBase() (RAG语义搜索)
      → AgentContext 构建
      → AgentEngine.execute()
        → MemoryManager (10轮窗口 + 超限摘要压缩)
        → PromptBuilder (OpenAI兼容消息数组构建)
        → LlmCaller (HttpURLConnection流式 → SseEmitter)
      → AgentCallback 保存消息 + 自动更新会话标题
```

### 6.3 数据库设计 (实际)

4 张表已建表（`sql/ai_agent.sql`）：
- `ai_conversation` — 对话会话表
- `ai_conversation_message` — 会话消息表（含 tool_calls JSON 预留）
- `ai_message_kb_ref` — 消息知识库引用表
- `ai_user_memory` — 用户长期记忆表（仅建表，自动逻辑 Phase 5）

### 6.4 前端页面结构 (实际)

```
views/ai/agent/
├── index.vue                     ✅ 292行，三栏布局主页面
└── components/
    ├── ConversationSidebar.vue   ✅ 91行，新建/删除/选择会话
    ├── ChatWindow.vue            ✅ 177行，SSE流式读取+Markdown渲染
    ├── AgentSettings.vue         ✅ 101行，模型/提示词/知识库/记忆设置
    ├── MessageBubble.vue         ✅ 127行，角色区分+tool_calls展示
    ├── ModelSelector.vue         ✅ 91行，下拉选择器
    └── ToolResultCard.vue        ✅ 76行，工具调用结果卡片
```

### 6.5 API设计 (实际)

| 接口 | 方法 | 路径 | 说明 | 状态 |
|------|------|------|------|:--:|
| 会话列表 | GET | /ai/agent/conversation/list | 会话列表 | ✅ |
| 新建会话 | POST | /ai/agent/conversation | 新建会话 | ✅ |
| 删除会话 | DELETE | /ai/agent/conversation/{ids} | 删除+级联消息 | ✅ |
| 清空上下文 | POST | /ai/agent/conversation/{id}/clear | 清空对话 | ✅ |
| 消息列表 | GET | /ai/agent/message/list/{id} | 获取消息 | ✅ |
| 发送消息(SSE) | POST | /ai/agent/chat | 流式对话 | ✅ |
| 停止生成 | POST | /ai/agent/chat/stop | 中断SSE流 | ✅ |
| 知识库搜索 | POST | /ai/agent/search | 语义搜索 | ✅ |
| 记忆列表 | GET | /ai/agent/memory/list | 长期记忆列表 | ✅ |
| 删除记忆 | DELETE | /ai/agent/memory/{ids} | 删除记忆 | ✅ |

> 比规划多 2 个端点：知识库搜索、记忆管理端点。全部 10 个端点已实现。

### 6.6 后端文件清单 (实际)

```
ruoyi-system/src/main/java/com/ruoyi/system/
├── domain/
│   ├── AiConversation.java              ✅
│   ├── AiConversationMessage.java       ✅
│   ├── AiUserMemory.java                ✅
│   └── AiMessageKbRef.java              ✅
├── mapper/
│   ├── AiConversationMapper.java + .xml  ✅ (含 updateMessageCount)
│   ├── AiConversationMessageMapper + .xml ✅ (含 selectRecentMessages)
│   ├── AiUserMemoryMapper.java + .xml    ✅
│   └── AiMessageKbRefMapper.java + .xml  ✅ (含 batchInsert)
├── service/
│   ├── IAiConversationService.java      ✅
│   ├── IAiAgentService.java             ✅ (核心编排)
│   ├── IAiMemoryService.java            ✅ (基础CRUD)
│   ├── IEmbeddingService.java           ✅ (向量化+语义搜索)
│   └── impl/
│       ├── AiConversationServiceImpl.java  ✅ (84行,@Transactional)
│       ├── AiAgentServiceImpl.java         ✅ (189行,完整编排)
│       ├── AiMemoryServiceImpl.java        ✅ (51行,Phase5待增强)
│       └── EmbeddingServiceImpl.java       ✅ (267行,余弦相似度)
├── controller/system/
│   └── AiAgentController.java           ✅ (157行,10端点)
└── agent/
    ├── AgentEngine.java                 ✅ (113行,编排引擎+回调)
    ├── AgentContext.java                ✅ (103行,Builder模式)
    ├── MemoryManager.java               ✅ (185行,窗口管理+摘要压缩)
    ├── PromptBuilder.java               ✅ (140行,消息数组构建)
    ├── LlmCaller.java                   ✅ (213行,HttpURLConnection流式)
    └── tool/                            ❌ 未创建 (Phase 4)

sql/
├── ai_agent.sql             ✅ (4张表)
└── ai_agent_menu.sql        ✅ (菜单+4权限)
```

### 6.7 权限标识

| 权限标识 | 说明 |
|---------|------|
| ai:agent:list | Agent菜单查看 |
| ai:agent:query | Agent查询 |
| ai:agent:add | 新建会话 |
| ai:agent:remove | 删除会话 |
| ai:agent:chat | 发送消息 |

---

## 七、Phase 4: 工具与联网检索

> **前置状态：** Agent 核心引擎已完成，`tool/` 子目录、工具注册表、Function Calling 待开发。

### 7.1 功能目标

- 创建 `agent/tool/` 子模块（ToolRegistry + AbstractTool + ToolHandler 接口）
- 实现 Function Calling 协议支持（OpenAI tool_calls 解析与执行）
- 内置工具：联网搜索 (DuckDuckGo/Bing API)、知识库查询、日期计算
- 工具执行结果可视化（前端 ToolResultCard 已就位）
- `ai_tool` 表建表 + 工具注册 CRUD
- 前端工具管理页面

### 7.2 数据库设计

```sql
-- 工具注册表
CREATE TABLE ai_tool (
  tool_id         BIGINT(20)   NOT NULL AUTO_INCREMENT  COMMENT '工具ID',
  tool_name       VARCHAR(100) NOT NULL                 COMMENT '工具名称',
  tool_code       VARCHAR(50)  NOT NULL                 COMMENT '工具代码',
  tool_desc       VARCHAR(500) DEFAULT NULL             COMMENT '工具描述',
  function_schema JSON         NOT NULL                 COMMENT 'Function Calling Schema定义',
  handler_class   VARCHAR(200) NOT NULL                 COMMENT '处理类全限定名',
  is_builtin      CHAR(1)      DEFAULT '1'              COMMENT '是否内置(0否 1是)',
  status          CHAR(1)      DEFAULT '0'              COMMENT '状态(0启用 1停用)',
  sort            INT(4)       DEFAULT 0                COMMENT '排序',
  create_by       VARCHAR(64)  DEFAULT ''               COMMENT '创建者',
  create_time     DATETIME                              COMMENT '创建时间',
  PRIMARY KEY (tool_id),
  UNIQUE KEY uk_tool_code (tool_code)
) ENGINE=InnoDB COMMENT='AI工具注册表';
```

### 7.3 内置工具列表

| 工具代码 | 工具名称 | 说明 |
|---------|---------|------|
| web_search | 联网搜索 | 通过搜索引擎检索最新信息 |
| kb_query | 知识库查询 | 查询项目知识库相关内容 |
| content_audit | 内容审核 | 审核帖子/评论内容 |
| date_calc | 日期计算 | 计算日期差值等 |
| weather | 天气查询 | 查询指定城市天气(可选) |

### 7.4 联网搜索方案

```
方案A: 调用搜索引擎API (必应搜索API / SerpAPI)
方案B: 使用第三方搜索服务 (Tavily API)
方案C: 使用免费API (DuckDuckGo Instant Answer API)

推荐: 方案A/B 二选一，备选方案C
```

### 7.5 工具调用流程

```
用户输入 → Agent解析意图 → 判断是否需要工具
  ├── 不需要 → 直接LLM回复
  └── 需要 → Function Calling
              ├── 调用联网搜索
              ├── 调用知识库查询  
              └── 调用其他工具
              ↓
         收集工具结果 → 注入上下文 → LLM整合回复 → 返回用户
```

---

## 八、Phase 5: 补全与修复

> **定位：** 不引入新功能模块，聚焦修复 Phase 0-4 遗留问题，补全未完成端点与数据。  
> **设计选型：** P1 方案B（LLM 渲染）、P2 方案A（批量更新）、O2 方案B（Tesseract CLI）。

### 8.1 任务总览

| 编号 | 类型 | 来源 | 任务 | 涉及文件 | 预估 | 状态 |
|:--:|:--:|------|------|------|:--:|:--:|
| **5.1** | 🐛 修复 | Phase 0 | exportConfig HTTP 方法错误 | `api/ai/llmConfig.js` | 0.5 | ✅ |
| **5.2** | 🐛 修复 | Phase 1 | toString() templateType 重复 | `AiPromptTemplate.java` | 0.5 | ✅ |
| **5.3** | 📝 补全 | Phase 1 | 新增 `/changeStatus` 批量启用/停用端点 | Controller + Service + Mapper | 1 | 📋 |
| **5.4** | 📝 补全 | Phase 1 | 补全 comment_moderate + user_report 内置模板 | `ai_prompt_template.sql` + LLM 生成 | 1 | 📋 |
| **5.5** | 📝 补全 | Phase 1 | 新增 `/preview` LLM 渲染端点 | Controller + Service (调用 LLM) | 1 | 📋 |
| **5.6** | 🔧 增强 | Phase 2 | Tesseract CLI OCR 图片文本提取 | ParserService + pom.xml + CLI 配置 | 2 | 📋 |
| **合计** | | | | | **6** | **2/6** |

### 8.2 开发顺序

```
5.1 → 5.2 → 5.3 → 5.4 → 5.5 → 5.6
(先修 Bug → 补全 Phase 1 端点/模板 → OCR)
```

---

### 8.3 任务 5.1: 修复 exportConfig HTTP 方法 🔴 → ✅ 已完成

| 项 | 内容 |
|------|------|
| **问题** | `api/ai/llmConfig.js` 中 `exportConfig()` 函数的 HTTP 方法误写为 `delete` |
| **影响** | 用户无法导出 LLM 配置 |
| **修复** | 1 处字符串修改：`method: 'delete'` → `method: 'get'` |
| **文件** | `ruoyi-ui/src/api/ai/llmConfig.js` |
| **验证** | 前端点击「导出配置」按钮返回正常配置数据 |
| **依赖** | 无 |
| **完成时间** | 2026-05-24 |

### 8.4 任务 5.2: 修复 toString() 方法 🔴 → ✅ 已完成

| 项 | 内容 |
|------|------|
| **问题** | `AiPromptTemplate.java` 中 `toString()` 方法 `templateType` 字段重复输出，`sceneName` 遗漏 |
| **影响** | 日志中提示词模板信息不完整，排查困难 |
| **修复** | 移除重复的 `templateType`，补上 `sceneName` |
| **文件** | `ruoyi-system/src/main/java/com/ruoyi/system/domain/AiPromptTemplate.java` |
| **验证** | 编译通过，`toString()` 输出完整字段 |
| **依赖** | 无 |
| **完成时间** | 2026-05-24 |

---

### 8.5 任务 5.3: 新增 /changeStatus 批量端点 🟡

| 项 | 内容 |
|------|------|
| **接口** | `PUT /ai/prompt/changeStatus?ids=1,2&status=0` |
| **说明** | 批量启用/停用提示词模板，遵循若依标准批量操作模式 |
| **参数** | `ids`: 逗号分隔的模板ID列表, `status`: 目标状态(0启用 1停用) |

**涉及改动：**

```
Controller (AiPromptController.java)
└── + changeStatus(Long[] ids, String status)
    → 参数校验 (内置模板不可停用)
    → Service.changeStatus()

Service (IAiPromptTemplateService + Impl)
└── + int changeStatus(Long[] ids, String status)
    → Mapper.updateStatusByIds()

Mapper (AiPromptTemplateMapper + XML)
└── + int updateStatusByIds(@Param("ids") Long[] ids, 
                             @Param("status") String status,
                             @Param("updateBy") String updateBy)
    → XML: UPDATE ai_prompt_template SET status=#{status}
           WHERE template_id IN (...) AND is_builtin='0'
```

| **校验规则** | `is_builtin='1'` 的内置模板不可通过此接口停用 |
| **验证** | API 测试：批量启用/停用，内置模板保护验证 |
| **依赖** | 5.1, 5.2 |

### 8.6 任务 5.4: 补全内置模板 🟡

| 模板 | 场景代码 | 模板类型 | 预期 prompt_content 要素 |
|------|---------|:--:|------|
| **AI评论管理** | comment_moderate | 系统(0) | 评论审核规则、敏感词过滤策略、回复建议模板 |
| **举报分析** | user_report | 系统(0) | 举报类型分类、严重程度判定、处理建议 |

**生成流程：**
```
1. 调用现有 LLM 配置 (content_audit 模板作为语境参考)
2. 提示词元提示: "为校园墙场景生成{场景名称}的系统提示词，要求..."
3. 人工审核生成结果
4. 写入 SQL 脚本 → sql/ai_prompt_template.sql
```

**SQL 格式：**
```sql
-- AI评论管理 (第5条内置模板)
INSERT INTO ai_prompt_template (template_name, template_type, scene_code, scene_name, prompt_content, variables, status, is_builtin, sort, create_by, create_time)
VALUES ('AI评论管理', '0', 'comment_moderate', '评论管理', '<LLM生成>', '["user_comment","context"]', '0', '1', 5, 'admin', NOW());

-- 举报分析 (第6条内置模板)
INSERT INTO ai_prompt_template (template_name, template_type, scene_code, scene_name, prompt_content, variables, status, is_builtin, sort, create_by, create_time)
VALUES ('举报分析', '0', 'user_report', '举报分析', '<LLM生成>', '["report_reason","reported_content","user_history"]', '0', '1', 6, 'admin', NOW());
```

| **验证** | 执行 SQL 后数据库中可查询到 6 条内置模板 (原 4 + 新 2) |
| **依赖** | 5.3 (LLM 配置可用即可并行) |

### 8.7 任务 5.5: 新增 /preview LLM 渲染端点 🟡

| 项 | 内容 |
|------|------|
| **接口** | `POST /ai/prompt/preview` |
| **Body** | `{ "templateId": 1, "variables": { "user_comment": "测试评论", "context": "校园墙帖子" } }` |
| **说明** | 调用 LLM 将变量填充到模板并渲染完整提示词，返回给前端预览 |

**处理流程：**
```
前端选择模板 + 输入变量值 → POST /ai/prompt/preview
  → Controller.preview(templateId, variables)
    → Service 加载模板 → 构建含变量值的用户消息
    → 调用 LLM (system=模板内容, user=变量JSON) → 返回渲染结果
```

**涉及改动：**

```
Controller (AiPromptController.java)
└── + preview(@RequestBody Map<String, Object> body)
    → 校验 templateId 存在
    → Service.previewPrompt()

Service (IAiPromptTemplateService + Impl)
└── + String previewPrompt(Long templateId, Map<String, String> variables)
    → 查询模板 → 构建 LLM 请求 → 调用 LLM → 返回渲染文本
    → 使用当前默认 LLM 配置 (或指定)

前端 (views/ai/prompt/index.vue)
└── 模板编辑/查看对话框中增加「预览」按钮
    → 弹窗输入变量值 → 调用 API → 展示渲染结果
```

| **API 响应** | `{ "code": 200, "data": "渲染后的完整提示词文本" }` |
| **验证** | 选择含 `{{user_comment}}` 的模板，输入变量值，返回正确渲染的提示词 |
| **依赖** | 5.3 (先确保 LLM 配置体系正常) |

### 8.8 任务 5.6: Tesseract CLI OCR 图片文本提取 🔵

| 项 | 内容 |
|------|------|
| **依赖** | 系统安装 Tesseract OCR + 中文语言包 (chi_sim) |
| **方案** | Java `ProcessBuilder` 调用 `tesseract` CLI，捕获 stdout |
| **支持格式** | `.jpg`, `.jpeg`, `.png` |

**集成点：**

```
文件上传 (AiKnowledgeDocumentServiceImpl.upload)
  → 判断文件类型: isImage(docType) → OCR 分支
    → ProcessBuilder("tesseract", tmpFile.getPath(), "stdout", "-l", "chi_sim+eng")
    → 读取 stdout 作为 content_text
  → 其他类型: 走现有 PDFBox/POI 解析
  → 统一走文本分块 + 向量化流程
```

**涉及改动：**

```
AiDocumentParserServiceImpl.java
└── + String extractTextFromImage(byte[] imageBytes)
    → 写临时文件 → ProcessBuilder tesseract CLI → 读取输出 → 清理临时文件
    → 异常处理: CLI 未安装/超时 → error_msg 记录

pom.xml (可选)
└── 无需额外 Java 依赖，纯 CLI 调用

application.yml (可选)
└── campus:
      ocr:
        tesseract-path: "tesseract"   # 可配路径，默认系统 PATH
        languages: "chi_sim+eng"
        timeout-seconds: 30
```

| **配置检查** | 启动时检测 `tesseract --version`，不可用时 OCR 功能降级但不阻塞 |
| **验证** | 上传含中文文字的 jpg/png 图片，正确提取文本并存入分块 |

---

### 8.9 Phase 5 验收标准

- [x] `exportConfig` 方法返回正确数据
- [x] `toString()` 输出包含所有关键字段
- [ ] `/changeStatus` 批量变更状态且内置模板受保护
- [ ] 数据库包含 6 条内置模板 (4 原有 + 2 新增)
- [ ] `/preview` 正确渲染含变量的提示词
- [ ] 上传 jpg/png 图片后正确提取中文文本
- [ ] 整体编译通过，前端页面功能正常

---

## 九、开发路线图

| 阶段 | 模块 | 状态 | 完成日期 | 依赖 |
|------|------|:--:|---------|------|
| **Phase 0** | LLM大模型配置 | ✅ | 已完成 | — |
| **Phase 1** | 提示词管理 | ✅ | 2026-05-23 | Phase 0 |
| **Phase 2** | 知识库管理 | ✅ | 2026-05-23 | Phase 0 |
| **Phase 3** | AI Agent 控制台 | ✅ | 2026-05-23 | Phase 1 + Phase 2 |
| **Phase 4** | 工具与联网检索 | 📋 | — | Phase 3 |
| **Phase 5** | 补全与修复 | 🔧 | 2026-05-24 (开始) | Phase 0-4 |

**当前进度：Phase 0-3 已完成 → Phase 5 进行中 (2/6)**

---

## 十、菜单与权限规划

### 9.1 菜单结构

```
AI治理 (menu_type=M, parent_id=0)
├── LLM大模型配置    [✅ Phase 0]
├── 提示词管理        [✅ Phase 1]
├── 知识库管理        [✅ Phase 2]
├── AI Agent 控制台   [✅ Phase 3]
└── 工具管理          [📋 Phase 4]
```

### 9.2 权限汇总

| 模块 | 权限标识前缀 | 状态 |
|------|-------------|:--:|
| LLM配置 | `ai:llm:*` (list/add/edit/remove/export) | ✅ |
| 提示词管理 | `ai:prompt:*` (list/add/edit/remove/generate) | ✅ |
| 知识库管理 | `ai:knowledge:*` (list/query/add/edit/remove) + `ai:knowledge:doc:*` (list/upload/remove/reparse/search) | ✅ |
| Agent控制台 | `ai:agent:*` (list/query/add/remove/chat) | ✅ |
| 工具管理 | `ai:tool:*` | 📋 Phase 4 |

---

## 十一、技术选型

| 功能 | 技术方案 |
|------|---------|
| LLM调用 | RestTemplate + HttpURLConnection |
| 流式响应(SSE) | Spring MVC SseEmitter (无 WebFlux 依赖) |
| 文档解析 | Apache PDFBox 3.0.5 + POI |
| 向量化 | LLM Embedding API (EmbeddingServiceImpl) |
| 记忆管理 | MySQL持久化 + MemoryManager 内存窗口 |
| 联网搜索 | DuckDuckGo API / Bing Search API |
| 前端对话 | 自定义 Vue 6组件 |
| Markdown渲染 | 自定义轻量实现 (粗体/斜体/代码块) |

---

## 十二、验收标准

- [x] 提示词管理支持完整 CRUD 和 AI 生成/润色 ✅ Phase 1
- [x] 知识库支持文件上传、解析、检索 ✅ Phase 2
- [x] Agent支持多轮对话记忆，切换会话不丢失上下文 ✅ Phase 3
- [ ] 提示词预览（变量渲染）和批量状态变更 📋 Phase 5
- [ ] 6 个内置校园墙场景模板完整 📋 Phase 5
- [ ] jpg/png 图片 OCR 文本提取 📋 Phase 5
- [ ] Agent能正确调用联网搜索工具 📋 Phase 4
- [x] Agent能基于知识库进行 RAG 增强回答 ✅ Phase 3
- [x] SSE流式输出，打字机效果 ✅ Phase 3
- [ ] 工具调用过程和结果可追溯展示 📋 Phase 4
- [x] 对话历史可持久化和回放 ✅ Phase 3
- [x] 权限控制完善 ✅ (Phase 0-3 的权限已全部配置)
- [x] 界面交互流畅，符合校园墙风格 ✅ (卡片式管理 + 抽屉面板)

---

## 十三、偏差汇总与原始需求对齐

### 12.1 Phase 1 偏差明细

| 偏差项 | 规划 | 实际 | 影响 | 处理决策 |
|--------|------|------|------|----------|
| 内置模板数量 | 6个系统提示词 | 4个系统 + 2个用户 | 低 | 待补 2个 (comment_moderate, user_report) |
| API端点 | 9个 | 7个 | 低 | /preview 和 /changeStatus 待补 |
| 独立生成服务 | IAiPromptGenerateService | 合并到 IAiPromptTemplateService | 无 | 接受简化 |
| PromptGenerateVO | 独立VO | 通过 remark 传 mode | 无 | 接受简化 |
| 前端组件拆分 | 3个独立组件 | 单文件 index.vue | 无 | 功能完整，后续可按需拆分 |

### 12.2 Phase 2 偏差明细

| 偏差项 | 规划 | 实际 | 影响 | 处理决策 |
|--------|------|------|------|----------|
| 图片上传/OCR | .jpg/.png 支持 | 跳过 → Phase 5 实现 | 低 | Tesseract CLI OCR |
| 向量化存储 | Phase 2 实现 | 预留列 → Phase 3 已补全 | 已关闭 | EmbeddingServiceImpl 实现 |
| 语义搜索 | Phase 2 实现 | MySQL LIKE → Phase 3 已升级 | 已关闭 | EmbeddingServiceImpl 余弦相似度 |
| 前端组件拆分 | 3个独立组件 | 单文件 index.vue | 无 | 功能完整 |

### 12.3 Phase 3 偏差明细

| 偏差项 | 规划 | 实际 | 影响 | 处理决策 |
|--------|------|------|------|----------|
| tool/ 子模块 | 5个工具类 | 未创建 | 中 | Phase 4 实现 |
| 联网搜索工具 | Agent 内实现 | 未实现 | 中 | Phase 4 实现 |
| 长期记忆自动 | Phase 3 实现 | 仅建表+CRUD | 低 | Phase 5 补全 |
| SSE 实现方式 | WebFlux | SseEmitter (无WebFlux依赖) | 无 | 架构简化 |
| 向量数据库 | PGVector/Milvus | MySQL JSON + 内存计算 | 无 | 接受简化 |
| 前端 Markdown | marked.js | 自定义轻量实现 | 低 | Phase 5 可选升级 |

### 12.4 原始需求核心对齐检查

| 原始需求 | Phase 1 | Phase 2 | Phase 3 | 结论 |
|---------|:--:|:--:|:--:|------|
| 系统提示词和用户提示词分离编辑 | ✅ | — | — | 核心功能完整 |
| 变量占位符 {{}} 支持 | ✅ | — | — | 前端提取 + 元提示词要求 |
| AI一键生成提示词 | ✅ | — | — | 调用 LLM 生成 |
| AI润色提示词 | ✅ | — | — | 调用 LLM 润色 |
| 校园墙场景模板 | ⚠️ | — | — | 4/6，待补2个 |
| 多知识库管理 | — | ✅ | — | CRUD 完整 |
| 文件上传解析 | — | ✅ | — | PDFBox + POI |
| 文本分块 | — | ✅ | — | 500字/块 + 50字重叠 |
| 知识库检索 | — | ✅ | — | Embedding 语义搜索 |
| RAG 增强 | — | ✅ | ✅ | Phase 3 完成集成 |
| 多轮对话记忆 | — | — | ✅ | MemoryManager |
| SSE 流式响应 | — | — | ✅ | SseEmitter |
| 对话历史持久化 | — | — | ✅ | 完整 CRUD |
| 知识库语义搜索 | — | — | ✅ | EmbeddingServiceImpl |

> **总体评估：Phase 0-3 核心功能全部完成。遗留问题（tool/ 子模块、长期记忆自动、Phase 1 待补端点/模板）为中等以下优先级，列入 Phase 5 修复补全。**

---

**文档状态：** Phase 0-3 ✅ · Phase 4 📋 · Phase 5 🔧 2/6 (Bug修复完成，补全进行中)
