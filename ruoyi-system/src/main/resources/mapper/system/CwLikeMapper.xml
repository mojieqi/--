<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.CwLikeMapper">

    <resultMap type="CwLike" id="LikeResult">
        <id     property="likeId"     column="like_id"     />
        <result property="userId"     column="user_id"     />
        <result property="targetType" column="target_type" />
        <result property="targetId"   column="target_id"   />
        <result property="status"     column="status"      />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <!-- 查询用户对某目标的点赞记录 -->
    <select id="selectCwLike" parameterType="CwLike" resultMap="LikeResult">
        SELECT like_id, user_id, target_type, target_id, status, create_time, update_time
        FROM cw_like
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </select>

    <!-- 查询用户对多个目标的点赞状态 -->
    <select id="selectLikeStatusList" resultMap="LikeResult">
        SELECT like_id, user_id, target_type, target_id, status, create_time, update_time
        FROM cw_like
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND status = '0'
          AND target_id IN
        <foreach collection="targetIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 新增点赞记录 -->
    <insert id="insertCwLike" parameterType="CwLike" useGeneratedKeys="true" keyProperty="likeId">
        INSERT INTO cw_like (user_id, target_type, target_id, status, create_time)
        VALUES (#{userId}, #{targetType}, #{targetId}, #{status}, sysdate())
    </insert>

    <!-- 更新点赞状态(取消/重新点赞) -->
    <update id="updateCwLike" parameterType="CwLike">
        UPDATE cw_like
        SET status = #{status}, update_time = sysdate()
        WHERE like_id = #{likeId}
    </update>

</mapper>
